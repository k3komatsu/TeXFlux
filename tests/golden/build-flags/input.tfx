!flag{draft}{off}
!flag{handout}{on}
!flag{internal}{off}

@frame{Results}: |
    !when[or]{draft}{internal} >> \marginpar{re-measure before the talk}
    !items:
        - Throughput doubled
        - Latency unchanged
    !unless[or]{handout}{internal}: |
        \pause
        @{\small} >> \textit{Ask about the tail latency here.}
