!defmacro{smallred}{body}:
    @{\small\color{red}} >> !param{body}
!defmacro{box}{title}{body}:
    @infobox::
        - !param{title}
        - !param{body}
@frame{Macros}:
    !smallred:
        Hello
        World
    !smallred >> \TextCA{Important}
    !box{Result}:
        Long
        body
