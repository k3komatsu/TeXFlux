% Ordinary lines are raw TeX.
\section{TeXFlux}

@frame{A small example}:
    \vspace{-.5em}
    !items:
        - Keep the TeX-first model
        -<2->[A] Shorten structural boilerplate only
    \medskip
    $\alpha = N/K$